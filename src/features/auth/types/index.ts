/**
 * Auth Types Export
 * 
 * Central export point for all authentication and profile types
 */

// Re-export all profile types
export * from './profile.types';

// Re-export all auth types
export * from './auth.types';

// Re-export the existing types for backward compatibility
export * from '../types';